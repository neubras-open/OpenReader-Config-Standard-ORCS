# OpenReader-Config-Standard-ORCS
Open source standard for interoperable RFID/NFC reader configuration. Defines parameters such as output format, byte length, byte order, Wiegand, HEX/decimal, VID/PID, and HID/serial/keyboard modes, enabling any software to configure compatible readers in a simple, standardized way.
